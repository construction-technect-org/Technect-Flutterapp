import 'package:flutter/widgets.dart';

class ProspectScreen extends StatelessWidget {
  const ProspectScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Prospect Screen"));
  }
}
