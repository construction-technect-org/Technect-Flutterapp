import 'package:flutter/widgets.dart';

class QualifiedScreen extends StatelessWidget {
  const QualifiedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Qualified Screen"));
  }
}
