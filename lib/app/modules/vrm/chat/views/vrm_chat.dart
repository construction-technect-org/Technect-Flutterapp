import 'package:construction_technect/app/core/utils/imports.dart';

class VrmChat extends StatelessWidget {
  const VrmChat({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Vrm Chat")));
  }
}
